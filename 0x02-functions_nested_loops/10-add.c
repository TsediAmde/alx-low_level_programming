#include "main.h"
#include <stdio.h>
/**
* add - adds two integers and returns the result
* @a: fist integer
* @b: second integer
*
* Return: the result of the addition
*/
int add(int a, int b)
{
return (a + b);
}
