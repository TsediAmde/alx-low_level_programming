#include "main.h"
#include <stdio.h>
#include <string.h>
/**
* _strlen - a function that returns the length of a string
* @s: character parameter
*
* Return: result
*/
int _strlen(char *s)
{
int result = strlen(s);
return (result);
}
